@page
@model App.Client.WebUI.Pages.Books.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Book</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BookViewModel.BookId" />
            <div class="form-group">
                <label asp-for="BookViewModel.Title" class="control-label"></label>
                <input asp-for="BookViewModel.Title" class="form-control" />
                <span asp-validation-for="BookViewModel.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BookViewModel.Author" class="control-label"></label>
                <input asp-for="BookViewModel.Author" class="form-control" />
                <span asp-validation-for="BookViewModel.Author" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BookViewModel.PublishedDate" class="control-label"></label>
                <input asp-for="BookViewModel.PublishedDate" class="form-control" />
                <span asp-validation-for="BookViewModel.PublishedDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
